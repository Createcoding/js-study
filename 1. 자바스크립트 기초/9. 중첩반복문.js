

// 구구단 3단 출력


// var level = prompt('몇단인지 입력하세요');

// for(var line = 1; line<=9; line++){

// console.log(`${level} X ${line} = ${level * line}`);

// }

// console.log(`============================`);

// var line = 1;
// while(line<=9){
//     console.log(`3 X ${line} = ${3 * line}`);
//     line++;
// }


for (var level = 2; level <= 9; level++){

    for(var line = 1; line<=9; line++){

        console.log(`${level} X ${line} = ${level * line}`);
        }
        console.log(`============================`)
}